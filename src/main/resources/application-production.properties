# Production starts with schema only. Do not load demo users or sample expenses.
spring.sql.init.data-locations=classpath:sql/production-data.sql

# The refresh cookie must be Secure behind HTTPS in production.
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS}
